{
  "name": "@ch1tty/evidence-mcp",
  "version": "1.0.0",
  "description": "Ch1tty focused MCP surface \u2014 ChittyEvidence document ingest + canonical URIs (REST adapter)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "hono": ">=4.12.25",
    "@hono/node-server": ">=2.0.0"
  }
}
