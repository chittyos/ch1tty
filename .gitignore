node_modules/
dist/
*.tsbuildinfo
pnpm-lock.yaml
sim/results/
sim/results.latest.json
coverage/

# local deploy/secret-shaped artifacts — never commit
.deploy/

# Claude Code tooling (worktrees, session state) — never commit
.claude/
