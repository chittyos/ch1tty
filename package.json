{
  "name": "ch1tty",
  "version": "5.0.0-do",
  "description": "ChittyOS Universal MCP Gateway — Cloudflare Worker + per-session Durable Object, slim MCP (search/execute/code), code mode, Workers AI brain",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@chittyos/schema-client": "file:../../CHITTYFOUNDATION/chittyschema/clients/schema-client",
    "@modelcontextprotocol/sdk": "^1.25.0",
    "agents": "^0.16.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
